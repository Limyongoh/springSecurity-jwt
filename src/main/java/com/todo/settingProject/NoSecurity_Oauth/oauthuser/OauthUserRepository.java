package com.todo.settingProject.NoSecurity_Oauth.oauthuser;

import com.todo.settingProject.domain.entity.user.OauthId;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

//@Repository
public interface OauthUserRepository {//extends JpaRepository<OauthUser,Long> {
//    Optional<OauthUser> findByEmail(String email);
//
//    Optional<OauthUser> findByOauthId(OauthId oauthId);
}